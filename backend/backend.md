# Backend

This folder contains the Express API.

## Run the server

```bash
npm install
npm run dev
```

The template includes these GET endpoints:

- `GET http://localhost:3000/`
- `GET http://localhost:3000/api/hello`
- `GET http://localhost:3000/api/type/{id-or-name}` (proxies PokéAPI type data)